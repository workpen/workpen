[package]
name = "workpen"
version = "0.0.1"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
description = "Reserved."
publish = false
keywords = []
categories = []

[features]
default = []
gc = []
nono = ["dep:nono"]

[dependencies]
thiserror = "2"

[target.'cfg(unix)'.dependencies]
nono = { version = "0.76", optional = true, default-features = false }

[dev-dependencies]
tempfile = "3"

[[test]]
name = "gc_corpus"
required-features = ["gc"]

[[test]]
name = "nono_corpus"
required-features = ["nono"]
